<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:microcharts="clr-namespace:Microcharts.Forms;assembly=Microcharts.Forms"
             x:Class="LawTechTeam.Views.ReportsPage"
             xmlns:vm="clr-namespace:LawTechTeam.ViewModels"
             Title="{Binding Title}">
    
    <ContentPage.BindingContext>
        <vm:ReportsViewModel />
    </ContentPage.BindingContext>
    
    <ScrollView>
        <StackLayout Padding="10">
            <Label
                Text="From this screen you can view graphs generated by the results of surveys."
                Padding="0, 50, 0, 50"
                Style="{DynamicResource DefaultTextStyle}"
                HorizontalOptions="Center"
                HorizontalTextAlignment="Center"
            />
            <Button Text="Discrimination In Custody" Command="{Binding GraphClicked}" CommandParameter="1"></Button>
            <Button Text="Gender Distribution" Command="{Binding GraphClicked}" CommandParameter="2"></Button>
            <Button Text="Injuries In Custody" Command="{Binding GraphClicked}" CommandParameter="3"></Button>
            <Button Text="Past Racial Discrimination" Command="{Binding GraphClicked}" CommandParameter="4"></Button>
        </StackLayout>
    </ScrollView>
    
</ContentPage>
